import React, { useState } from 'react';

const Table = () => {
    return (
        <>
            <label>Table detection mechanism</label>
            <select>
                <option>Set rectangle</option>
                <option>Use image features</option>
            </select>
        </>
    )
};

export default Table;
